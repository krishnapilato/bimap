<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE xml>
<xml>
	<!-- Define Appenders: components responsible for outputting log messages -->
	<Appenders>
		<!-- Console Appender: Logs messages to the console (SYSTEM_OUT) -->
		<Console name="Console" target="SYSTEM_OUT">
			<PatternLayout
					pattern="%style{%d{ISO8601}}{black} %highlight{%-5level }[%style{%t}{bright,blue}] %style{%C{1.}}{bright,yellow}: %msg%n%throwable"/>
		</Console>

		<!-- RollingFile Appender: Logs messages to a file with rolling policies -->
		<RollingFile name="RollingFile"
					 fileName="C:\Simple Map Exercise\LOGGING.log"
					 filePattern="C:\Simple Map Exercise\LOGGING%d{-dd-MMMM-yyyy-%i.log.gz">
			<!-- File log pattern -->
			<PatternLayout>
				<pattern>[%d{HH:mm:ss.SSS}] [%t] %-5level %logger{36} - %msg%n</pattern>
			</PatternLayout>
			<!-- Rolling policies to control log rotation -->
			<Policies>
				<SizeBasedTriggeringPolicy size="25 MB" /> <!-- Rotate logs when size exceeds 25 MB -->
				<TimeBasedTriggeringPolicy /> <!-- Rotate logs based on time -->
			</Policies>
		</RollingFile>
	</Appenders>

	<!-- Define Loggers: specify logging levels and associate appenders -->
	<Loggers>
		<!-- Root Logger: Applies to all classes unless overridden -->
		<Root level="info">
			<AppenderRef ref="Console" />
			<AppenderRef ref="RollingFile" />
		</Root>

		<!-- Custom Logger for the 'com.onstairs' package -->
		<Logger name="com.onstairs" level="debug" />
	</Loggers>
</xml>