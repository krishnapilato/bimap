package com.bimap.business.modules.registry.dto;

/// How a client should render one form field.
/// @author Khova Krishna Pilato
public enum FieldType {
    TEXT,
    TEXTAREA,
    NUMBER,
    DECIMAL,
    SELECT,
    AUTOCOMPLETE,
    COORDINATE
}
